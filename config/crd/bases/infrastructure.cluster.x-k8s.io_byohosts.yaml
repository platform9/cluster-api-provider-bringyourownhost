---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: byohosts.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: ByoHost
    listKind: ByoHostList
    plural: byohosts
    shortNames:
      - byoh
    singular: byohost
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - jsonPath: .status.hostinfo.osname
          name: OSName
          type: string
        - jsonPath: .status.hostinfo.osimage
          name: OSImage
          type: string
        - jsonPath: .status.hostinfo.architecture
          name: Arch
          type: string
      name: v1beta1
      schema:
        openAPIV3Schema:
          description: ByoHost is the Schema for the byohosts API
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: ByoHostSpec defines the desired state of ByoHost
              properties:
                bootstrapSecret:
                  description: |-
                    BootstrapSecret is an optional reference to a Cluster API Secret
                    for bootstrap purpose
                  properties:
                    apiVersion:
                      description: API version of the referent.
                      type: string
                    fieldPath:
                      description: |-
                        If referring to a piece of an object instead of an entire object, this string
                        should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                        For example, if the object reference is to a container within a pod, this would take on a value like:
                        "spec.containers{name}" (where "name" refers to the name of the container that triggered
                        the event) or if no container name is specified "spec.containers[2]" (container with
                        index 2 in this pod). This syntax is chosen only to have some well-defined way of
                        referencing a part of an object.
                      type: string
                    kind:
                      description: |-
                        Kind of the referent.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                      type: string
                    name:
                      description: |-
                        Name of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                    namespace:
                      description: |-
                        Namespace of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                      type: string
                    resourceVersion:
                      description: |-
                        Specific resourceVersion to which this reference is made, if any.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                      type: string
                    uid:
                      description: |-
                        UID of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                desiredAgentPackageChecksum:
                  description: |-
                    DesiredAgentPackageChecksum, if set, is the expected checksum
                    ("sha256:<hex>") of the specific .deb/.rpm file extracted from the
                    DesiredAgentPackageURL bundle — not of the OCI image itself, which
                    digest-pinning the reference above already covers. The agent refuses
                    to install on mismatch.
                  type: string
                desiredAgentPackageURL:
                  description: |-
                    DesiredAgentPackageURL is an OCI image reference — pulled via `imgpkg
                    pull` — for a bundle containing the .deb/.rpm that installs
                    DesiredAgentVersion. Not templated and does not reference a Secret —
                    an image reference is not sensitive. The
                    agent extracts the bundle, picks the single *.deb or *.rpm inside it
                    matching its own already-known package manager, and installs it;
                    nothing else is executed. Never used to install a version older than
                    the one currently running — that always requires manual (SSH/Ansible)
                    intervention outside this field. Pinning this reference by digest
                    (@sha256:...) rather than a mutable tag is the primary integrity
                    mechanism; see DesiredAgentPackageChecksum for a secondary check.
                  type: string
                desiredAgentVersion:
                  description: |-
                    DesiredAgentVersion, when set, is the agent version this host should
                    be running. The agent compares this against its own
                    version.Get().GitVersion on every reconcile tick and only acts on a
                    mismatch. Unset means no managed upgrade is in progress.
                  type: string
                installationSecret:
                  description: |-
                    InstallationSecret is an optional reference to InstallationSecret
                    generated by InstallerController for K8s installation
                  properties:
                    apiVersion:
                      description: API version of the referent.
                      type: string
                    fieldPath:
                      description: |-
                        If referring to a piece of an object instead of an entire object, this string
                        should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                        For example, if the object reference is to a container within a pod, this would take on a value like:
                        "spec.containers{name}" (where "name" refers to the name of the container that triggered
                        the event) or if no container name is specified "spec.containers[2]" (container with
                        index 2 in this pod). This syntax is chosen only to have some well-defined way of
                        referencing a part of an object.
                      type: string
                    kind:
                      description: |-
                        Kind of the referent.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                      type: string
                    name:
                      description: |-
                        Name of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                    namespace:
                      description: |-
                        Namespace of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                      type: string
                    resourceVersion:
                      description: |-
                        Specific resourceVersion to which this reference is made, if any.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                      type: string
                    uid:
                      description: |-
                        UID of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                uninstallationSecret:
                  description: |-
                    UninstallationScript is an optional field to store uninstall script
                    generated by InstallerController
                    UninstallationScript *string `json:"uninstallationScript,omitempty"`
                  properties:
                    apiVersion:
                      description: API version of the referent.
                      type: string
                    fieldPath:
                      description: |-
                        If referring to a piece of an object instead of an entire object, this string
                        should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                        For example, if the object reference is to a container within a pod, this would take on a value like:
                        "spec.containers{name}" (where "name" refers to the name of the container that triggered
                        the event) or if no container name is specified "spec.containers[2]" (container with
                        index 2 in this pod). This syntax is chosen only to have some well-defined way of
                        referencing a part of an object.
                      type: string
                    kind:
                      description: |-
                        Kind of the referent.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                      type: string
                    name:
                      description: |-
                        Name of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                    namespace:
                      description: |-
                        Namespace of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                      type: string
                    resourceVersion:
                      description: |-
                        Specific resourceVersion to which this reference is made, if any.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                      type: string
                    uid:
                      description: |-
                        UID of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
              type: object
            status:
              description: ByoHostStatus defines the observed state of ByoHost
              properties:
                agentVersion:
                  description: |-
                    AgentVersion is the version.GitVersion of the agent binary currently
                    running on this host, as reported by the agent itself. The management
                    cluster only reads it.
                  type: string
                conditions:
                  description: Conditions defines current service state of the BYOMachine.
                  items:
                    description: Condition defines an observation of a Cluster API resource operational state.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is the last time the condition transitioned from one status to another.
                          This should be when the underlying condition changed. If that is not known, then using the time when
                          the API field changed is acceptable.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message is a human readable message indicating details about the transition.
                          This field may be empty.
                        maxLength: 10240
                        minLength: 1
                        type: string
                      reason:
                        description: |-
                          reason is the reason for the condition's last transition in CamelCase.
                          The specific API may choose whether or not this field is considered a guaranteed API.
                          This field may be empty.
                        maxLength: 256
                        minLength: 1
                        type: string
                      severity:
                        description: |-
                          severity provides an explicit classification of Reason code, so the users or machines can immediately
                          understand the current situation and act accordingly.
                          The Severity field MUST be set only when Status=False.
                        maxLength: 32
                        type: string
                      status:
                        description: status of the condition, one of True, False, Unknown.
                        type: string
                      type:
                        description: |-
                          type of condition in CamelCase or in foo.example.com/CamelCase.
                          Many .condition.type values are consistent across resources like Available, but because arbitrary conditions
                          can be useful (see .node.status.conditions), the ability to deconflict is important.
                        maxLength: 256
                        minLength: 1
                        type: string
                    required:
                      - lastTransitionTime
                      - status
                      - type
                    type: object
                  type: array
                hostinfo:
                  description: HostDetails returns the platform details of the host.
                  properties:
                    architecture:
                      description: The Architecture reported by the host.
                      type: string
                    osimage:
                      description: OS Image reported by the host.
                      type: string
                    osname:
                      description: The Operating System reported by the host.
                      type: string
                  type: object
                lastHeartbeatTime:
                  description: |-
                    LastHeartbeatTime is the last time the agent running on this host
                    confirmed it was alive. clusterv1.Condition has no per-condition
                    heartbeat/last-probe-time field (unlike corev1.NodeCondition), so this
                    is tracked as a top-level status field rather than as part of the
                    AgentConnected condition below. The management-cluster ByoHostReconciler
                    compares this against its configured HeartbeatTimeoutPeriod to compute
                    AgentConnected; it does not read or write this field itself — only the
                    host agent does.
                  format: date-time
                  type: string
                machineID:
                  description: |-
                    MachineID uniquely identifies the underlying machine this host runs on,
                    read from /etc/machine-id (falling back per systemd's machine-id(5)
                    convention where absent) and reported by the agent itself. Unlike the
                    ByoHost name — derived from os.Hostname(), which can change, collide, or
                    be reused across a re-provisioned machine — this does not change unless
                    the machine itself is reimaged. It is a fact, not an enforcement
                    mechanism: this ADR only surfaces it, it does not compare it against
                    anything or reject a mismatch.
                  type: string
                machineRef:
                  description: |-
                    MachineRef is an optional reference to a Cluster API Machine
                    using this host.
                  properties:
                    apiVersion:
                      description: API version of the referent.
                      type: string
                    fieldPath:
                      description: |-
                        If referring to a piece of an object instead of an entire object, this string
                        should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                        For example, if the object reference is to a container within a pod, this would take on a value like:
                        "spec.containers{name}" (where "name" refers to the name of the container that triggered
                        the event) or if no container name is specified "spec.containers[2]" (container with
                        index 2 in this pod). This syntax is chosen only to have some well-defined way of
                        referencing a part of an object.
                      type: string
                    kind:
                      description: |-
                        Kind of the referent.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                      type: string
                    name:
                      description: |-
                        Name of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                    namespace:
                      description: |-
                        Namespace of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                      type: string
                    resourceVersion:
                      description: |-
                        Specific resourceVersion to which this reference is made, if any.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                      type: string
                    uid:
                      description: |-
                        UID of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                network:
                  description: |-
                    Network returns the network status for each of the host's configured
                    network interfaces.
                  items:
                    description: NetworkStatus provides information about one of a VM's networks.
                    properties:
                      connected:
                        description: |-
                          Connected is a flag that indicates whether this network is currently
                          connected to the VM.
                        type: boolean
                      ipAddrs:
                        description: IPAddrs is one or more IP addresses reported by vm-tools.
                        items:
                          type: string
                        type: array
                      isDefault:
                        description: |-
                          IsDefault is a flag that indicates whether this interface name is where
                          the default gateway sit on.
                        type: boolean
                      macAddr:
                        description: MACAddr is the MAC address of the network device.
                        type: string
                      networkInterfaceName:
                        description: NetworkInterfaceName is the name of the network interface.
                        type: string
                    required:
                      - macAddr
                    type: object
                  type: array
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
